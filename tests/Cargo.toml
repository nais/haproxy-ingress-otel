[package]
name = "haproxy-otel-tests"
version = "0.0.0"
edition = "2021"
publish = false

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
wiremock = "0.5"
serde_json = "1"
